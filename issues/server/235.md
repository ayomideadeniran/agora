# Issue 235: Docker Compose for Local Development

### DESCRIPTION

> **Note:** Continuous Integration (CI) must pass for any submission. Contributors are required to write and pass unit tests for their specific changes.

> Provide a `docker-compose.yml` file to help new contributors spin up a local PostgreSQL instance easily.

### TASKS

- [ ] Create `server/docker-compose.yml` with a `postgres:latest` image.
- [ ] Configure default environment variables for the database.
- [ ] Add a `volumes` section to persist data locally.
- [ ] **Unit Tests**: Implement corresponding unit tests (e.g., using `tokio::test`) to verify your changes.
- [ ] **CI Compliance**: Ensure all GitHub CI checks pass (run `cargo test` and `cargo clippy` locally).

**Requirement:** Make sure GitHub CI check passes before submitting.

**Depends on:**
None
